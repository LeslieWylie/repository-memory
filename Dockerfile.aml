FROM python:3.12-slim

WORKDIR /app
ENV PYTHONUNBUFFERED=1
ENV XDG_DATA_HOME=/data

COPY . .
RUN python -m pip install --no-cache-dir .

RUN mkdir -p /data/repository-memory
EXPOSE 8080

CMD ["repository-memory-aml", "--host", "0.0.0.0", "--port", "8080"]
