diff --git a/src/core/state/local-backend.ts b/src/core/state/local-backend.ts
index 355a337..0a33de4 100644
--- a/src/core/state/local-backend.ts
+++ b/src/core/state/local-backend.ts
@@ -106,7 +106,7 @@ export class LocalStateBackend implements IStateBackend {

     const delay = Math.max(0, fireAtMs - Date.now());
     const handle = this.onTimerExpired
-      ? setTimeout(() => { this.timers.delete(key); this.onTimerExpired!({ member, fireAtMs }); }, delay)
+      ? setTimeout(() => { this.timers.delete(key); this.onTimerExpired!({ instanceId, member, fireAtMs }); }, delay)
       : undefined;
     if (handle) handle.unref();
     this.timers.set(key, { member, fireAtMs, handle });
@@ -131,7 +131,7 @@ export class LocalStateBackend implements IStateBackend {
     const expired: TimerEntry[] = [];
     for (const [key, timer] of this.timers) {
       if (key.startsWith(prefix) && timer.fireAtMs <= nowMs) {
-        expired.push({ member: timer.member, fireAtMs: timer.fireAtMs });
+        expired.push({ instanceId, member: timer.member, fireAtMs: timer.fireAtMs });
       }
     }
     for (const entry of expired) {
diff --git a/src/core/state/types.ts b/src/core/state/types.ts
index d1cae8f..49f1cf7 100644
--- a/src/core/state/types.ts
+++ b/src/core/state/types.ts
@@ -46,6 +46,8 @@ export const DEFAULT_PIPELINE_STATE: PipelineSessionState = {
 // ============================

 export interface TimerEntry {
+  /** The state-backend instance that owns this timer. */
+  instanceId: string;
   member: string;
   fireAtMs: number;
 }
diff --git a/src/gateway/server.ts b/src/gateway/server.ts
index ecca673..4a1d1e7 100644
--- a/src/gateway/server.ts
+++ b/src/gateway/server.ts
@@ -1595,7 +1595,7 @@ export class TdaiGateway {
           // or legacy "session:L2_schedule"
           const member = entry.member;
           let taskType: string;
-          let instanceId: string;
+          let instanceId: string = entry.instanceId;
           let sessionId: string;
           let teamId: string | undefined;
           let agentId: string | undefined;
@@ -1633,7 +1633,7 @@ export class TdaiGateway {
             taskType = parsed.taskType;
             teamId = parsed.teamId;
             agentId = parsed.agentId;
-            instanceId = this.config.instanceId ?? "default";
+            instanceId = instanceId || this.config.instanceId || "default";
           }
           const now = Date.now();
           // Extract targetMmdFile from member for offload-l2 (needed by pipeline-worker lockKey)

diff --git a/src/utils/pipeline-factory.ts b/src/utils/pipeline-factory.ts
--- a/src/utils/pipeline-factory.ts
+++ b/src/utils/pipeline-factory.ts
@@ -843,4 +843,11 @@ export function createL3Runner(opts: {
   const { pluginDataDir, cfg, openclawConfig, vectorStore, logger, instanceId, llmRunner, storage } = opts;

   return async () => {
+    // repository-memory owns L3 acceptance.  In that integration, ordinary
+    // conversation processing may create L2 scenarios, but must never
+    // silently promote a profile; the explicit core/write path does that.
+    if (process.env.REPOSITORY_MEMORY_DISABLE_AUTO_L3 === "1") {
+      logger.debug?.(`${TAG} [L3] Automatic persona generation disabled by repository-memory policy`);
+      return;
+    }
    const scopes = await discoverProfileScopes(pluginDataDir, storage, logger);
