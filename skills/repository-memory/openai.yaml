interface:
  display_name: "Repository Memory"
  short_description: "Find cited project facts and durable memory"
  default_prompt: "Use $repository-memory to check source-backed evidence before answering this project question."

policy:
  allow_implicit_invocation: true
